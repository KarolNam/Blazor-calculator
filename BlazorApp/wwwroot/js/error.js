function showAlert(liczba) {
    alert(`Nie możesz podzielić liczby ${liczba} przez zero.`);
}
function showError(liczba) {
    alert(`Nie znaleziono operacji o numerze ${liczba}. Dostępne operacje 1 - dodawanie, 2 - odejmowanie, 3 - mnożenie, 4 - dzielenie.`);
}